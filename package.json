{
  "name": "nurul-fashion-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Root package untuk Vercel build (build Vite di UrbanStyle-Catalog)",
  "scripts": {
    "dev": "npm --prefix UrbanStyle-Catalog run dev",
    "build": "npm --prefix UrbanStyle-Catalog install && npm --prefix UrbanStyle-Catalog run build",
    "preview": "npm --prefix UrbanStyle-Catalog run preview"
  }
}

